export enum MenuTypeEnum {
  MENU_GROUP = 0, // 目录
  MENU = 1, // 菜单
  PERMISSION = 2, // 权限
}

export enum MenuExtOpenModeEnum {
  OUTER = 1, // 外链
  INNER = 2, // 内链
}

export enum MenuKeepAliveEnum {
  KEEP_ALIVE = 1, // 缓存
  NOT_KEEP_ALIVE = 0, // 不缓存
}

export enum MenuShowEnum {
  SHOW = 1, // 显示
  HIDE = 0, // 隐藏
}

export enum MenuStatusEnum {
  ENABLE = 1, // 启用
  DISABLE = 0, // 禁用
}
